{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Backend: Stop (Port 8000)",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-Command",
        "$targets = Get-CimInstance Win32_Process | Where-Object { ($_.CommandLine -match 'uvicorn') -and ($_.CommandLine -match '8000') }; if ($targets) { foreach ($t in $targets) { Stop-Process -Id $t.ProcessId -Force -ErrorAction SilentlyContinue; Write-Output \"Stopped backend process PID: $($t.ProcessId)\" } } else { $conn = Get-NetTCPConnection -LocalPort 8000 -State Listen -ErrorAction SilentlyContinue; if ($conn) { $pids = $conn | Select-Object -ExpandProperty OwningProcess -Unique; foreach ($p in $pids) { Stop-Process -Id $p -Force -ErrorAction SilentlyContinue; Write-Output \"Stopped backend listener PID: $p\" } } else { Write-Output \"No backend process listening on port 8000\" } }"
      ],
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "focus": true
      }
    },
    {
      "label": "Backend: Start (uvicorn)",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-Command",
        "Set-Location '${workspaceFolder}'; python -m uvicorn backend.main:app --reload --host 127.0.0.1 --port 8000"
      ],
      "isBackground": true,
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "focus": true
      }
    },
    {
      "label": "Backend: Restart (FastAPI)",
      "type": "shell",
      "command": "powershell",
      "args": [
        "-NoProfile",
        "-ExecutionPolicy",
        "Bypass",
        "-Command",
        "$targets = Get-CimInstance Win32_Process | Where-Object { ($_.CommandLine -match 'uvicorn') -and ($_.CommandLine -match '8000') }; if ($targets) { foreach ($t in $targets) { Stop-Process -Id $t.ProcessId -Force -ErrorAction SilentlyContinue } }; $conn = Get-NetTCPConnection -LocalPort 8000 -State Listen -ErrorAction SilentlyContinue; if ($conn) { $pids = $conn | Select-Object -ExpandProperty OwningProcess -Unique; foreach ($p in $pids) { Stop-Process -Id $p -Force -ErrorAction SilentlyContinue } }; Start-Sleep -Milliseconds 500; Set-Location '${workspaceFolder}\\backend'; & '.\\.venv\\Scripts\\python.exe' -B -m uvicorn main:app --host 127.0.0.1 --port 8000"
      ],
      "isBackground": true,
      "problemMatcher": [],
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "focus": true
      },
      "group": "build"
    }
  ]
}
